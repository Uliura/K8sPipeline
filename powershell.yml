steps:
    - task: AzureCLI@2
      inputs:
        azureSubscription: 'AzureSC'
        scriptType: 'bash'
        scriptLocation: 'scriptPath'
        scriptPath: 'infra.sh'
        addSpnToEnvironment: true
        useGlobalConfig: true

