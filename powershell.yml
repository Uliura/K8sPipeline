steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: 'apt-get install openssh-server -y  && ssh-keygen -m PEM -t rsa -b 4096'
      workingDirectory: '$(System.WorkFolder)'
      failOnStderr: true
  - task: AzurePowerShell@5
    inputs:
      azureSubscription: 'Free Subscription(664b98cb-cc33-4ac5-a541-f8cf0203bb97)'
      ScriptType: 'FilePath'
      ScriptPath: 'infra.ps1'
      azurePowerShellVersion: 'LatestVersion'
      pwsh: true