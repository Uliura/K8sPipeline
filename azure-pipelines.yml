trigger: none
variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'dockersc'
  imageRepository: 'pipelinerepa'
  containerRegistry: 'aksagent.azurecr.io'
  dockerfilePath: '**/Dockerfile'
  tag: '$(Build.BuildId)'
  imagePullSecret: 'pipelinerepogeka-auth'
  kubernetesconnection: 'kubeconnection'
  resourcegroupname: "AzureDevOps"
  aksclustename: "mynewpwshclaster"
  azurelocation: "eastus"      

  
stages:

  - stage: powershell
    displayName: Build infrastructure
    jobs:
    - job: Powershell
      displayName: Powershell Infra
      pool:
        name: My-AKS-Pool
      steps:
        - task: AuthenticatedPowerShell@1  
          inputs:
            serviceConnection: 'AzureDevOpsServCon'
            targetType: inline
            script: 'Write-Host "url: $env:AS_SC_URL | username: $env:AS_SC_USERNAME | password: $env:AS_SC_PASSWORD"'
        - task: AzurePowerShell@5
          inputs:
            azureSubscription: 'AzureDevOpsServCon'
            ScriptType: 'InlineScript'
            Inline: |
              New-AzAksCluster -ResourceGroupName $(resourcegroupname) -Name $(aksclustename) -Location $(azurelocation) -NodeCount 1 -GenerateSshKey -Force
            azurePowerShellVersion: 'LatestVersion'
            pwsh: true
            
  - stage: BuildDocker
    displayName: Build docker
    jobs:
    - job: builddocker
      displayName: Build docker
      pool:
        name: Azure Pipelines
      steps:
        - template: dockerbuild.yml
        - task: PublishPipelineArtifact@1
          inputs:
            artifactName: 'allmanifests.yaml'
            path: './'

  - stage: DeployDocker
    displayName: Deploy docker
    jobs:
    - job: DeployDocker
      displayName: Deploy docker
      pool:
        name: My-AKS-Pool
      steps:
          - task: DownloadPipelineArtifact@2
            inputs:
              artifactName: 'lmanifests.yaml'
              downloadPath: '$(System.ArtifactsDirectory)'
          - task: AzurePowerShell@5
            inputs:
              azureSubscription: 'AzureDevOpsServCon'
              ScriptType: 'InlineScript'
              Inline: |
                Install-AzAksKubectl -Force
                Import-AzAksCredential -ResourceGroupName $(resourcegroupname) -Name $(aksclustename) -Force
                kubectl apply -f allmanifest.yaml
              azurePowerShellVersion: 'LatestVersion'
              pwsh: true
          